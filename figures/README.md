# 论文图片

正文默认使用 `framework.tex` 中的矢量技术路线示例，无需外部图片。

放入 `framework.pdf` 或 `framework.png` 后会自动替换示例；两者同时存在时优先使用 PDF。其他图片可用 `\includegraphics` 插入。建议采用 PDF、PNG 或 JPEG，避免依赖 EPS 自动转换。

文件名尽量使用简短英文和连字符。`main.pdf` 等编译产物被忽略，但本目录中的论文图片 PDF 会正常纳入 Git。
